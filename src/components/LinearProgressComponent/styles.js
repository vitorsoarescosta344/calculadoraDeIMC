import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  gradientBar: {width: '100%', height: 40, borderRadius: 10},
  cursorStyle: {
    height: 40,
    width: 5,
    backgroundColor: 'black',
    position: 'absolute',
  },
});
